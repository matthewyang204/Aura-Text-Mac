# Aura Text for Mac
This is Aura Text ported to macOS. It is a project still in progress. Visit OLDREADME.md if you want to read through the original README of the main repository.

cd into the unzipped directory and run `sh setup.sh` to install.
